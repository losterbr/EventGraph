#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT_DIR"

if [ -x "$ROOT_DIR/scripts/quality.sh" ]; then
  "$ROOT_DIR/scripts/quality.sh"
else
  echo "scripts/quality.sh not found or not executable." >&2
  exit 1
fi
